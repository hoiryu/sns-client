import Container from '~stories/ui/containers/Container';
import TextFieldSearch from '../inputs/TextFieldSearch';

const SearchSidebar = () => {
	return (
		<Container>
			<TextFieldSearch />
		</Container>
	);
};

export default SearchSidebar;

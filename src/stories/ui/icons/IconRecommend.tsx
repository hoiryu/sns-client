import Icon from '@mui/icons-material/Recommend';
import { ISvgProps } from '~models/ui/image';

const IconRecommend = ({ ...props }: ISvgProps) => <Icon {...props} />;

export default IconRecommend;

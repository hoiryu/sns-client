'use client';

interface IProps {
	username: string;
}

const ContainerPosts = ({ username }: IProps) => {
	// const query = postsService.getPostsByUsername(username);
	return null;
	// return <ListWindowScrollPosts query={query} />;
};

export default ContainerPosts;
